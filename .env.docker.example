# 复制为 .env 后由 docker compose 读取（与 docker-compose.yml 同目录）
# copy 为 .env: cp .env.docker.example .env

MYSQL_DATABASE=auto_amazon
MYSQL_USER=auto_amazon
MYSQL_PASSWORD=请改成强密码
MYSQL_ROOT_PASSWORD=请改成另一强密码

# 宿主机访问 Nginx 的端口
HTTP_PORT=8080
